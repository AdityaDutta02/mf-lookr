# Dependencies
node_modules/

# Next.js build output — NEVER commit these
.next/
out/

# Build artifacts
dist/
build/

# Environment variables — NEVER commit these
.env
.env.local
.env.*.local
.env.production

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo

# Local extraction toolkit outputs (raw factsheets, parsed data) — large,
# not part of the deployed app; uploaded to Terminal AI storage/DB instead.
tools/cache/
tools/out/
tools/.venv/
tools/__pycache__/
